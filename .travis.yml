language: cpp
include:
  - os: linux
    addons:
      apt:
        sources:
          - ubuntu-toolchain-r-test
compiler:
  - gcc
script:
  - mkdir build
  - cd build
  - cmake ..
  - make 
  - make test

