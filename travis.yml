language: cpp
compiler:
  - gcc
  - clang
script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make test
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
